.card
  .card-header
    .fa.fa-user
    - if user.online?
      Online 
    - else
      Offline
    = link_to user.email, user_path(user)
  .card-footer
    Signed up
    = time_ago_in_words(user.created_at)
    ago
  .card-footer
    = 'Contagem de logins: '.humanize
    = user.sign_in_count
    .row
    = 'Login atual em: '.humanize
    = user.current_sign_in_at
    .row
    = 'Último login em: '.humanize
    = user.last_sign_in_at
    .row
    = 'IP do login atual: '.humanize
    = user.current_sign_in_ip
    .row
    = 'IP do último login: '.humanize
    = user.last_sign_in_ip

    .card-footer
      - user.roles.each do |role|
        = role.name
      - if policy(user).edit?
        = link_to 'Editar atribuições', edit_user_path(user), class: 'btn btn-sm btn-warning'