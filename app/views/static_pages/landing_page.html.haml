.jumbotron.d-flex.flex-column
  .display-6
    Prod-E, seu aprendizado em análise
  %hr
    .my-4
  - if current_user
    = search_form_for @ransack_courses, as: :courses_search, url: courses_path do |f|
      .input-group
        = f.search_field :titulo_cont, class: 'form-control-sm', style: 'float: left; width: 150px; ', placeholder: 'Pesquisar curso', :autocomplete => :off
        %span.input-group-append
          %button.btn.btn-primary.btn-sm{:type => "submit"}
            Pesquisar

.card-group.p-4
  .card.card-body
    %h1
      .fa.fa-graduation-cap
    = Course.count
    Cursos
    .row
    Cursos disponíveis
  .card.card-body
    %h1
    = Course.distinct.count(:user_id)
    Professores
    .row
    Encontre o seu professor
  .card.card-body
    %h1
    = User.count
    Estudantes
    .row
    Interaja com seus novos colegas para ampliar seu conhecimento


%p

%h2
  .fa.fa-user-graduate
  Continue Aprendendo
.card-columns
  - @enrolled_courses.each do |course|
    = render 'courses/course', course: course


%hr
%h2 
  Cursos mais bem avaliados
.card-columns
  - @top_rated_courses.each do |course|
    = render 'courses/course', course: course

%hr
